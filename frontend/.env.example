# Frontend environment variables (Vite)
# Copy this file to `.env.local` and update as needed.
# Never commit `.env.local` to version control — it's in .gitignore.

VITE_API_URL=http://localhost:5001
